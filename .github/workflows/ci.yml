name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - work
      - 'codex/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

<<<<<<< codex/test-.gitkeep-usage-may93s
      - name: Syntax check
        run: python3 -m py_compile word_upload_demo.py test_word_upload_demo.py

=======
>>>>>>> main
      - name: Run unit tests
        run: python3 -m unittest -v
